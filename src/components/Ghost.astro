---
const LIVES = 3;
---

<section class="life" aria-hidden="true">
	{Array.from({ length: LIVES }, () => <span class="heart" data-ghost-heart />)}
	<p class="life-label" data-ghost-gameover hidden>-Game Over-</p>
</section>
<figure class="ghost" aria-hidden="true" data-ghost-sprite></figure>

<script>
import { initGhost } from "../lib/ghost";

let cleanup: (() => void) | undefined;

document.addEventListener("astro:page-load", () => {
	cleanup?.();
	cleanup = initGhost(document);
});
</script>

<style>
.ghost {
	background: transparent url(/images/ghost.webp);
	background-size: contain;
	top: -125px;
	height: 125px;
	left: -125px;
	opacity: 0.5;
	perspective: 800px;
	position: absolute;
	transform-style: preserve-3d;
	transition:
		left 7s,
		top 7s,
		transform 1s;
	width: 125px;
	margin: 0;
	&:hover {
		opacity: 1;
	}
	@media screen and (max-width: 650px) {
		display: none;
	}
}
.ghost.flipped {
	transform: rotateY(180deg);
}

.life {
	position: absolute;
	left: 1rem;
	top: 1rem;
	text-transform: uppercase;
	@media screen and (max-width: 650px) {
		display: none;
	}
}
.life-label {
	position: absolute;
	top: 0;
	transition: opacity 2s ease;
	white-space: nowrap;
	margin: 0;
}
.heart {
	background: url(/images/heart.webp);
	background-size: contain;
	display: inline-block;
	height: 45px;
	margin-right: 0.5rem;
	transition: opacity 0.5s ease;
	width: 50px;
}
.heart[hidden],
.life-label[hidden] {
	display: none;
}

/* The ghost only exists to chase the cursor, so there is nothing left to show. */
@media (prefers-reduced-motion: reduce) {
	.ghost,
	.life {
		display: none;
	}
}
</style>
